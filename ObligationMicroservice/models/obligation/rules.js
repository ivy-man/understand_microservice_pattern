let rules = {
    ps: 'required|integer',
    pst: 'required|integer',
    ga: 'required|integer',
    gad: 'required|integer',
    du: 'required|integer',
    date: 'required|date'
};

module.exports = rules;